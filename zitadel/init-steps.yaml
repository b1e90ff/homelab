# All possible options and their defaults: https://github.com/zitadel/zitadel/blob/main/cmd/setup/steps.yaml
FirstInstance:
  Org:
    Human:
      Username: '${SECRET_ZITADEL_FIRST_INSTANCE_ORG_HUMAN_USERNAME}'
      Password: '${SECRET_ZITADEL_FIRST_INSTANCE_ORG_HUMAN_PASSWORD}'