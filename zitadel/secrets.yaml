# All possible options and their defaults: https://github.com/zitadel/zitadel/blob/main/cmd/defaults.yaml

# If not using the docker compose example, adjust these values for connecting ZITADEL to your CockroachDB
Database:
  cockroach:
    User:
      Username: "${SECRET_ZITADEL_FIRSTINSTANCE_ORG_HUMAN_USERNAME}"
    Admin:
      Username: "${SECRET_ZITADEL_COCKROACH_USERNAME}"